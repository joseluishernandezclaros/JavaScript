
// let cadena = "cadena de prueba prueba prueba";
// let cadena2 = "cadena";

// resultado = cadena.concat(cadena2); //concatenar o unir cadenas
// resultado = cadena.startsWith(cadena2);  //verificar si una cadena comienza con la letra de otra cadena
// resultado = cadena.endsWith(cadena2); //si las cadenas terminan con la misma letra
// resultado = cadena.includes(cadena2); //si una cena contiene o incluye a otra cadena 
// resultado = cadena.indexOf("de"); //posicion numerica de una cadena en especifico
// resultado = cadena.lastIndexOf("prueba"); //ultima posicion en que se encuentra la palabra buscada
// resultado = cadena.padStart(60,"prueba "); //agrega una cadena al inicio y se le define una cantidad de caracteres
// resultado = cadena.padEnd(60," prueba "); //agrega una cadena al final y se le define una cantidad de caracteres
// resultado = cadena.repeat(2); //repite una cadena una cantidad x de veces


// document.write(resultado);

let modcadena = "          Hola como           estas";
// let modcadena2 = "cadena";

// resultado = modcadena.split("como"); //se divide cada vez que encuentre el elemento definido
// resultado = modcadena.substring(0,4); //definir una seccion para que muestre el texto en esa ubicacion
// resultado = modcadena.toLowerCase(); //convierte cadena a minuescula
// resultado = modcadena.toUpperCase(); //convierte todo a mayuscula
// resultado = modcadena.toString(); //convertir a string
// resultado = modcadena.trim(); //cortar los eementos que seleccionemos
// resultado = modcadena.trimEnd(); //quita espacio final
// resultado = modcadena.trimStart(); //quita espacio inicio


document.write(resultado.length);